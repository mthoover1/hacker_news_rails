<table>
  <tr>
    <td>user:</td>
    <td><%=@user.username%></td>
  </tr>
  <tr>
    <td>created:</td>
    <td><%=@user.created_at%></td>
  </tr>
  <tr>
    <td></td>
    <td><%= link_to "Submissions", user_posts_path(@user) %></td>
  </tr>
  <tr>
    <td></td>
    <td><%= link_to "Comments", user_comments_path(@user) %></td>
  </tr>
</table>
<%= @user.inspect %><br>
<%= session[:user_id].inspect %>
<%=current_user.inspect%>
